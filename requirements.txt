# --- Core framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Validation & settings ---
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1

# --- HTTP requests (para adapters LLM) ---
requests==2.32.3
types-requests==2.32.4.20250913  # Type stubs for requests

# --- (Opcional pero recomendado) Tests, formato y linting ---
pytest==8.3.3
httpx==0.27.2        # Cliente HTTP async para tests
black==24.8.0        # Formateador de código
ruff==0.6.2          # Linter rápido (reemplaza flake8 + isort)
mypy==1.13.0         # Chequeo de tipos estáticos

# --- (Opcional) OpenAI SDK (para el futuro adapter GPT) ---
openai==1.46.0